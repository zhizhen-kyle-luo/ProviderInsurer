# Audit Log Viewer

professional React-based viewer for debugging multi-agent healthcare simulation audit logs

## Usage

run run_experiment.py to gain audit log json. Expect 10+ minutes runtime for longer runs.

```bash
cd audit_viewer
npm install
npm run dev
```

open http://localhost:3000, follow instruction on screen and upload an audit log json to view!
